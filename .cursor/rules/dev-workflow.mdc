---
description: AI Whisper 開發流程規範
globs: "**/*.py"
---

# 開發流程規範

## 觸發時機

以下兩種情況都必須執行重啟：

1. 每次修改完 .py 程式碼後，必須自動重啟 AI Whisper 讓改動生效，不需要等使用者要求。
2. 使用者傳送「重啟」、「restart」、「重新啟動」，立即執行重啟。

## 重啟流程

**永遠只使用 `restart-with-log.ps1`**，不要直接執行 `py main.py`（會產生多餘的 CMD 視窗）。

### Step 1：執行重啟腳本（block_until_ms: 15000 等待輸出）

```powershell
powershell -ExecutionPolicy Bypass -File "restart-with-log.ps1"
```

**重要參數：**
- `block_until_ms: 15000`：等待腳本完成（腳本內含 4 秒等待 + 讀 log）
- `working_directory`：指向 workspace 根目錄，不要寫死磁碟代號（家裡 F:\、公司 D:\）
- 腳本已包含：終止舊程序 → 啟動新程序 → 等待 4 秒 → 顯示 log 末 15 行

### Step 2：確認啟動成功

確認輸出中有出現 `[main][HH:MM:SS] ✅ 快捷鍵` 等啟動訊息。

**若 log 為空：不代表啟動失敗**，只是程序還沒輸出，不要額外再執行 `py main.py`。

## LOG 監控

- log 寫入 `ai_whisper_yyyyMMdd_HHmmss.log`（workspace 根目錄）
- 若使用者說「幫我看 LOG」、「看 log」，讀取最新的 `ai_whisper_*.log` 檔

## 注意事項

- 使用 PowerShell，不使用 `&&`
- 不要直接執行 `py main.py`，一律透過 `restart-with-log.ps1`

## 程式碼品質

- 若有 linter 紅線（型別錯誤、未定義變數等），**立即修正，不需等使用者要求**。
- PIL / Pillow 的 `color` 參數型別問題，統一補上 `# type: ignore[arg-type]`。
- 其他無法根本修正的型別誤報，補上對應的 `# type: ignore[...]` 標註。
